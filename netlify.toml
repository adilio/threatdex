[build]
  command = "pnpm build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "24"

# React Router SSR - Netlify will serve static files first
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/react-router-server"
  status = 200
